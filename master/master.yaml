apiVersion: batch/v1
kind: Job
metadata:
  name: master
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: master
          image: marcomicera/broker-test-rabbitmq-master:responses
          imagePullPolicy: Always
